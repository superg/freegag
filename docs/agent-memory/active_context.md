# Active context

- The Windows compilation failure in `portable_path.h` is fixed. `normalize_host_path()` now explicitly converts `std::filesystem::path::preferred_separator` to the `char` value type used by `path.string()` before passing it to `std::replace`; this avoids conflicting `char`/`wchar_t` template deduction on MSVC while preserving the existing Linux behavior. The full Debug build, `check-format`, and `git diff --check` pass.
- A complete Linux playthrough now finishes with no visible bugs observed by the user. The intermittent duplicate software cursor was a cross-thread scene-selection race: SDL mouse motion could acquire the old cursor scene, the script thread could park it and publish the replacement cursor, and the late motion update could then move the old scene back onscreen permanently. Runtime pointer movement, cursor-scene switching, and suspend/resume deferred-scene bookkeeping now share one recursive mutex, making each cursor-state transition atomic without changing input behavior, cursor selection, scene timing, or game mechanics. The Debug build, `check-format`, and `git diff --check` pass, and the user completed the game under GDB without another visible issue. This state is marked by the annotated Git tag `linux-playthrough-complete`.
- The castle guard dialogue response highlights are fixed and user-confirmed. Pointer hit testing and click dispatch were working; the response script drives three separate hover animations with `/PLAY:i_HLTn:RESTART:ONE_STEP`. A readability cleanup had changed the recovered animation state-word update from preserving the supplied flags to `(state != 0 ? RUNTIME_MEDIA_PAUSED : 0)`, so the `0x20200` restart/one-step request became a pause and no highlight frame was presented. `set_runtime_resource_state()` now uses the exact simplified form of the recovered operation: clear the existing paused/one-step bits, then OR the complete requested state word. Normal play (`0`) and pause (`1`) are unchanged; script-driven restart and one-step animation controls are restored across the game. Bitmap, sound, input, hit-testing, and click paths are untouched. The Debug build and `check-format` target pass, `git diff --check` passes, and the user confirmed correct hover behavior for all response options.
- The XTET post-match queued-key burst is fixed deterministically and user-confirmed. The remaining root cause was input-triggered match animation executing synchronously inside SDL's main-thread event callback: four 400-ms animation waits prevented SDL from pumping X11 keyboard input, so it accumulated outside the application and arrived after the match. XTET's animation delay is now a host service. Worker-thread matches retain an ordinary delay, while a main-thread match wait pumps SDL every millisecond and flushes keyboard/text events without dispatching them into gameplay; the deadline preserves the original 400-ms frame timing. The post-match request/completion/release generations still cover SDL callback-prefetched events and worker-triggered matches, but release deterministically after the presenter-service drain instead of using the former 10-ms quiet-gap heuristic. Rapid Down input throughout a match animation no longer moves the following figurine in the assisted runtime test. All temporary diagnostics are removed. The Debug `all` and `check-format` targets build, and `git diff --check` passes.
- The fly minigame freeze after two catches on lowest difficulty is fixed in code, pending user runtime confirmation. The user's paused GDB process showed the script worker waiting for resource count 28 while the concrete count was permanently 27 and the deferred-destruction queue was empty; the stranded non-primary `i_MDSIT` (`FLMx-ur.flc`) animation was at frame 0 with `RUNTIME_MEDIA_RESOURCE_PENDING | RUNTIME_MEDIA_PAUSED | RUNTIME_MEDIA_PAUSE_NOTIFIED`. The Win32 logic normally consumed a non-deferred secondary animation's initial pending registration before script-driven pause, but standard-thread scheduling made the reverse ordering reachable. `update_runtime_resource_animation_backend()` now consumes that one-time pending registration from an early pause callback for deferred-load or non-primary animations, retaining the original half-size exclusion. An initial version applied this to non-deferred primary animations too and caused a startup black screen because the primary constructor snapshots its target count only after starting the worker; the final condition keeps non-deferred primary animations on their original ordering path. This changes no playback, difficulty, timing, or fly rules. The game and `check-format` targets build successfully, `git diff --check` passes, and Codex did not launch the game.
- Linux window-position persistence now prefers SDL's X11 backend with native Wayland as fallback (`x11,wayland`) at default hint priority, so an explicit `SDL_VIDEO_DRIVER` environment setting still wins. This is required because Wayland intentionally withholds top-level window coordinates and SDL consequently reports a synthetic display-origin position; when XWayland is available, the existing shutdown-time rectangle persistence can now save and restore real coordinates. The game target and `check-format` build successfully. Per user direction, Codex did not launch the game; manual move/close/relaunch confirmation is pending. Native Wayland users who explicitly select `wayland`, or lack XWayland, can retain window size but cannot have the application restore an exact compositor-controlled position.
- The Linux Continue/resume crash caused by `source=gag01.cdf` failing to match host file `Gag01.cdf` is fixed in code, pending user runtime confirmation. `portable_path.h` resolves every component of an existing host path with exact-case priority and deterministic ASCII-insensitive fallback, normalizes both slash styles, and is used by archive/shared-file input, standalone resource detection, archive validation, autosave/preference/save discovery, XTET SFS loading, and extractor input; existing preference/CDF outputs resolve before overwrite so casing differences do not create duplicate files. Legacy directory/file-name parsing now recognizes both separators. CDF entry lookup was already ASCII-insensitive; runtime file-cache and generic-script-resource identities now are too. All temporary `[resume-trace]` diagnostics are removed. The Linux Debug game, XTET library, and extractor build; `check-format` and `git diff --check` pass. A read-only mixed-case component probe (`DATA/gAg01.CdF`) reached the file and rejected its contents as non-PE, confirming host resolution. Per user direction, Codex must not launch the game.
- The startup-video X cursor and nonfunctional clicks after the unused-field cleanup were caused by removing three members that participate in an intentional cross-type layout view: runtime hit testing reinterprets `RuntimeTreeLink84` records as `RuntimePointerRegion`. This shifted coordinates, scene masks, cursor visuals, and owner pointers. The fields are restored with role-based alias names (`pointer_current_scene_bit`, `link_movement_flags`, and `link_movement_deadline`), and compile-time `offsetof`/size assertions now verify every field consumed through that view. Debug build, all three CTests, `check-format`, and `git diff --check` pass; manual startup-video cursor and click confirmation is pending.
- A repository-wide unused-code/layout pass removed 18 unused functions or methods, including dead XTET rendering/accessor helpers, obsolete media callbacks, the producerless legacy runtime-message queue and its empty palette-mode path, and unused overloads. It also removed unused compressor globals, two unnecessary local/data structures, one dead function-pointer alias, 11 unused enum members, and nonserialized/write-only fields across application, display, media, runtime tree, text, save/load, and XTET state. The script field-query snapshot was reduced to actual existence validation, and the queued application payload now uses the already parsed names. All 17 genuine one-member enums became fixed-width `inline constexpr` constants. Remaining unread fields are limited to raw CDF/MVZ/WAV/PE file overlays or verified layout aliases (`RuntimeExpandedListResource`, `RuntimePlanNode`, and the `RuntimeTreeLink84`/`RuntimePointerRegion` view). Debug and Release builds of all targets, the Debug `/W4` diagnostic pass, all three CTests, `check-format`, declaration/one-member-enum/stale-symbol audits, `git diff --check`, and LF audit pass.
- The one-member `CdfEntryFlag` enum has been removed. The serialized entry byte still combines its low-bit `RuntimeMediaDataType` value with the independent `inline constexpr uint8_t CDF_ENTRY_COMPRESSED = 1u << 4` modifier; all existing compression tests, sets, and clears retain the same on-disk representation. Debug build, `check-format`, stale-symbol audit, `git diff --check`, and LF audit pass.
- The generic `binary_stream` hierarchy and `binary_stream.*` have been removed. `CdfArchive` is opaque outside `cdf_archive.cpp`; normal input uses a private `SharedBinaryFile` plus archive-local offset, alternate input uses explicitly owned async records, and output is an owned `std::fstream`. CDF entry-stream lookup now accepts a name and resolves offsets inside the archive implementation, eliminating exposed `CdfEntry` layout, virtual dispatch, implementation wrappers, and `dynamic_cast`. Standalone resource classification reads its 16-byte header through `std::ifstream`. The narrowly scoped `SharedBinaryFile` retains a mutex-protected `read_at` because duplicated asynchronous records share one physical stream while maintaining independent logical offsets. Debug build, `check-format`, all three CTests, normal/XTET five-second startup smokes, stale-symbol and opaque-layout audits, `git diff --check`, and LF audit pass.
- `RuntimeSoundHandleValue`, `RUNTIME_SOUND_HANDLE_NONE`, and `RUNTIME_SOUND_HANDLE_UNAVAILABLE` have been removed. Valid handles remain exactly `1..1023`, while zero is now the sole invalid/failure representation: disabled or invalid-format creation and slot exhaustion both return zero, and animation backends retain their zero-allocated initial sound handle until an audio format chunk creates one. This aligns with existing `handle != 0` consumers and prevents `UINT32_MAX` exhaustion from being mistaken for success; the XTET callback boundary remains `uint32_t`. Debug build, `check-format`, stale-symbol audit, `git diff --check`, and LF audit pass.
- The misleading `RuntimeSoundControlValue` enum has been removed. Its numerically identical but semantically separate values are independent `uint32_t` constants: `RUNTIME_SOUND_LOOP_INFINITE` for the loop-count domain and `RUNTIME_SOUND_PLAYBACK_MARKER_PRESET` for the nonzero, non-timestamp marker installed on a newly constructed paused/one-step sound. Audio loop logic now compares against the sound-specific constant instead of the unrelated generic infinite-wait sentinel, and the old `RUNTIME_SOUND_MARKER_UNSET` name is gone. Debug build, `check-format`, stale-symbol audit, and `git diff --check` pass.
- XTET's mouse down-arrow control is intentionally edge-triggered, matching the original DLL. Read-only Ghidra verification of `GAME_DLL_WND_PROC` and `HandleGameMouseButton` shows that `WM_LBUTTONDOWN` hit-tests the control and invokes one downward gameplay move, while `WM_LBUTTONUP` performs no move; the original has no held-mouse state or mouse-repeat timer. Keyboard Down repeats only because Windows/SDL supplies repeated key-down events, and Space remains the explicit hard drop. No runtime source change was made.
- The synthesized `FGGAGBOY.CFG` startup event now uses `/RELOADNOFADE:GAGBOY` instead of `/PRELOAD:GAGBOY`. This retains the reload-style tree switch while explicitly selecting the immediate transition, preventing the runtime from randomly choosing its enabled rectangle wipe when XTET starts. The Debug build, `check-format`, and `git diff --check` pass; visual confirmation of the XTET launch remains pending.
- The `--xtet` startup override now changes only the selected configuration filename to `FGGAGBOY.CFG`. Its embedded GAGBoy script moved from `application.cpp` into the `extra/synthesized_resource.cpp` registry as `RUNTIME_MEDIA_DATA_CONFIGURATION`; normal resource detection first checks the CDF and, because `FGGAGBOY.CFG` is absent from `Gag01.cdf`, uses the existing archive-miss synthesis path to classify and load the bytes. The old `GAGBOY.CFG` virtual-script interception, `gagboy_startup_mode`, and one-use `has_xtet_argument` helper are gone; argument scanning is local to startup, normal `Start.cfg`/archive configuration selection is unchanged, XTET pause/resume follows the standard host path, and `SAVELOAD.CFG` virtual handling remains intact. Debug build, `check-format`, CDF/name/mode audits, `git diff --check`, and LF audit pass. Isolated normal and `--xtet` smoke launches remained alive through initialization for 4 and 6 seconds respectively and created canonical INI files in build-only working directories.
- The obsolete fixed `application_message_table[23][0x104]` and all 20 unused recovered menu/setup/display strings have been removed. The two live diagnostics now pass their complete text directly to `std::fputs`; no named string constants remain. `ApplicationState::message_table`, its initialization, and the `application_message` fixed-stride pointer helper are also gone. Debug build, `check-format`, message-table audit, `git diff --check`, and LF audit pass.
- The heterogeneous `validate_startup_environment` stage-mask dispatcher has been removed rather than preserved with an enum. Startup explicitly loads preferences, calls `disable_unavailable_saved_game_actions`, and validates/selects `Gag01.cdf`; resource-path validation and the new-game/resume/credits retry paths call `validate_and_select_application_archive` with an explicit diagnostic choice. Fullscreen availability is assigned directly at its sole call. Shadowed `0x80`, never-passed `0x400`, the broad `0xfffef9ff` historical mask, recurring `0x24`, the `stages` parameter, and the obsolete public declaration are gone. Debug build, `check-format`, stage-mask audit, `git diff --check`, and LF audit pass.
- A project-wide one-statement-function audit removed 36 dead or low-use wrappers and accessors. One- and two-use operations are now direct at their consumers across application flags/property storage, runtime mutexes and scene synchronization, animation pause/resume and MVZ selection, save capture/serialization/write, CDF metadata, resource scene-format selection, runtime-tree parsing/lookups, generic-backend flags, XTET timing/accessors, and synthesized bitmap row access. The synthesized blits now calculate flipped source/destination rows once per row rather than through per-pixel helpers. Remaining low-use one-statement functions are deliberate boundaries: program entry/destruction, callbacks, private SDL/binary-stream/game-state access, the private scene parser facade, legacy heap semantics, and the named legacy sound scheduling formula. Debug build, `check-format`, removed-symbol/remaining-candidate audits, `git diff --check`, and LF audit pass.
- The four graphics/resource-host settings now use behavior-derived names through storage, aliases, script-property dispatch, transition consumers, and async-host updates: `runtime_palette_transition_step`, `runtime_rectangle_transition_step_size`, `runtime_available_scene_transitions`, and `runtime_resource_stream_rate_bytes_per_millisecond`. Their uppercase named defaults are 6 channel levels per 2-ms palette update, rectangle setting 5 (normalized to an 8-by-6-pixel update), `RUNTIME_SCENE_TRANSITION_IMMEDIATE | RUNTIME_SCENE_TRANSITION_RECTANGLE`, and 1700 bytes/ms. The setter's former raw `0xf` is `RUNTIME_SCENE_TRANSITION_AVAILABILITY_MASK`; serialized `PALFADE`/`FRAMEFADE` text remains unchanged. The palette path is currently unreachable through normal selection because the XRGB host is 32-bpp and palette transitions are disabled unless the host is 8-bpp. Debug build, `check-format`, stale-name audit, and `git diff --check` pass.
- The XTET first-match freeze was traced to SDL event starvation, not gameplay settlement. During a worker-thread match animation, XTET holds its game mutex and can block after filling the three-snapshot presentation queue. A concurrent held Down-key event failed the UI thread's nonblocking XTET lock and was immediately pushed back into SDL; that self-sustaining retry stream prevented SDL's iterate callback from servicing queued snapshots, so the worker and UI could not progress while audio continued independently. Contended gameplay input is now consumed instead of requeued, consistent with the existing post-match keyboard flush; the now-unused success/retry return from the host input dispatcher was removed. Temporary trace source has been removed. Debug and Release builds pass; user confirmation across the first match is pending. The generated `data/freegag-trace.log` remains temporarily because another process still holds it open and should be deleted once released.
- The recovered 35-slot `runtime_game_host_callbacks` DLL interface has been replaced with the six-field typed `xtet::GameHostServices` boundary: dirty-region invalidation plus create/destroy/queue/pause/resume sound operations. Dead media, resource-host, asynchronous-file, and CDF entries; the reserved slot gap; numeric lookup/copying; global table initialization/reset; and all function-pointer-to-`void *` casts are gone. The reversed audio control names now reflect behavior: `pause_runtime_sound` selects the non-advancing state and `resume_runtime_sound` selects the advancing state, with XTET's `AudioHostCallbacks` using the same terminology. `queue_runtime_sound_data` now accepts immutable sample data. The duplicate XTET context layout was replaced by an alias to the shared `RuntimeGameHostContext`, removing the remaining context reinterpret-cast and obsolete DLL initializer typedef. Debug and Release builds, `check-format`, stale-interface/name audits, `git diff --check`, and LF audit pass.
- The script-runtime property callback protocol is fully typed with scoped `ScriptRuntimeProperty` members for palette/rectangle fade duration, transition options, resource path/data/release/frame, shared value, pointer coordinates, resource-host mode, suspension/property-state scopes, tree destruction, and missing source/section failures. `ScriptRuntimeRoot`, setters/getters, parser dispatch, serialization, tree lifecycle, resource model, and expression queries all use the enum; no numeric property call remains. Callback signatures now match exactly, direct assignment replaces the former `memcpy` installation, the getter reinterpret-cast adapter and forwarding wrappers are gone, and the setter's universally zero unused argument was removed. The unhandled operation-48 tree-created notification was confirmed to be a no-op and removed. Debug build, `check-format`, `git diff --check`, and LF audit pass.
- Application and scripted-save field updates now use the existing script protocol names `SCRIPT_BOOLEAN_TRUE` (`0x03000000`), `SCRIPT_BOOLEAN_FALSE` (`0x07000000`), and `SCRIPT_VALUE_TYPE_BOOLEAN` instead of raw payloads and type code `1`; the runtime's string field update similarly uses `SCRIPT_VALUE_TYPE_STRING`. Boolean payload locals are named `script_boolean_value`, and `resolve_state_field_reference` now accepts `ScriptValueType` rather than an untyped integer. Command names were corrected to match the encoded result semantics (`QUERY_*_DISABLED`, `QUERY_SOUND_MUTED`, and `QUERY_FULLSCREEN_UNAVAILABLE`). Numerically similar `0x30000000`/`0x70000000` values are distinct scope/opcode protocols and were not conflated with the boolean encoding. Debug build, `check-format`, `git diff --check`, and LF audit pass.
- `HostApplicationCommand` now names every command handled by `handle_application_host_event`, including availability/preference queries, new/load/save/resume actions, fullscreen/audio/comment toggles, snapshot lifecycle, and the existing subsystem events. The handler converts the raw script/event command once and switches exclusively on enum members; its internal comparisons and the fullscreen-send latch use the same names. The event boundary retains `uint32_t` because scripted save/load owns additional message values handled before the application switch. Debug build, `check-format`, `git diff --check`, and LF audit pass.
- The dead `SecondaryWindowLayout` type and forward declaration have been removed. `update_application_window_layout` no longer accepts an always-null secondary-layout parameter, and its unreachable non-null branch is gone; both call sites use the simplified one-argument function. Debug build, `check-format`, `git diff --check`, and LF audit pass.
- Every independent member of `ApplicationFlag` now uses an explicit unsigned bit expression (`1u << bit`) rather than a hexadecimal literal. Bit positions and behavior are unchanged. Debug build, `check-format`, `git diff --check`, and LF audit pass.
- The redundant `ApplicationValidationFlag` enum and `ApplicationState::validation_flags` field have been removed. Validation no longer accumulates unused stage bits, and the three always-true runtime-readiness conditions were simplified; every actual validation-stage check and side effect remains unchanged. Debug build, `check-format`, `git diff --check`, and LF audit pass.
- The unused `GraphicsHostInitializationFlag` and `GRAPHICS_HOST_DISPLAY_MODE_MASK` have been removed. The corresponding flags parameter on `initialize_graphics_host` and ignored options parameter on `initialize_sdl_presenter` are gone; graphics initialization now assigns the sole observed state, `RUNTIME_TARGET_ACTIVE`, directly to `runtime_target_flags`. Debug build, `check-format`, `git diff --check`, and LF audit pass.
- The unused `GraphicsHostInitializationResult` success-token struct and `graphics_host_state` global have been removed. `initialize_graphics_host` and `initialize_runtime_graphics` now return `bool`; the never-read `bits_per_pixel` result field is gone. `ApplicationState::game_context` and the ignored context parameters in screenshot/save-state capture were removed through the complete call chain. Debug build, `check-format`, `git diff --check`, and LF audit pass.
- All 116 fixed internal `*Api` structs and their singleton forwarding objects have been removed. Their call sites now invoke the underlying typed functions directly, including runtime-tree destruction, resource/media construction, script parsing, display/application lifecycle, archive access, save persistence, and XTET integration. Fixed single-function indirections (`compressed_cdf_reader`, runtime state transition, and credits completion) and adapter-only accessors/wrappers were also inlined. The real display synchronizer remains a named callback plus context rather than a one-member API struct; synthesized resources now use their sole CDF source directly. A source audit finds no `*Api` types or `_api` objects, while data-owned display traversal, animation, script-runtime, and XTET callbacks remain dynamic. The clean Debug build passes.
- Empty braces on a field declaration such as `uint8_t rgbBlue{};` are default member initializers: they make plain default construction (`BitmapColor color;`) initialize that field to zero. They are not required for layout or binary overlay use, and `BitmapColor` itself is never directly constructed in current source—it is only addressed through binary-data pointers and `sizeof`—so its four initializers are behaviorally redundant. Removing default member initializers project-wide would not be safe without a type-by-type construction audit because many state/owning structs rely on plain construction, whereas binary layout records can generally use explicit `{}` at the object construction site when zeroing is required. No source change has yet been made for this question.
- Magic return/status protocols have been separated into named domains. CDF I/O uses `CdfError`; save enumeration uses `ArchiveCommentEnumerationResult`; display target and traversal callbacks use `DisplayTargetResult` and `DisplayTraversalResult`; script parsing uses named end, invalid-integer, value-type, and encoded-boolean values; generic text uses named directives and position/integer sentinels; sound handles/loop/markers, resource frame limits, movement deadlines, resource-token outcomes, generic-child queries, pointer-outside input, root scene indices, and runtime media data types each have purpose-specific names. Raw resource playback composites now use the existing media flags. Two validation callbacks whose numeric returns were discarded (`load_local_preferences` and `enable_borderless_fullscreen`) are now `void`. A source audit finds no remaining hexadecimal status/result returns; the hexadecimal returns in `translate_sdl_keycode` are intentional legacy keycode payload mappings. A clean Debug rebuild, `check-format`, `git diff --check`, uppercase-enum audit, and LF audit pass.
- Display-scene operation statuses use the unscoped `DisplayOperationResult` enum: `DISPLAY_OPERATION_SUCCESS`, `DISPLAY_OPERATION_RELEASE_PENDING`, `DISPLAY_OPERATION_LOCK_NOT_OWNED`, and `DISPLAY_OPERATION_FAILED`. Release-pending deliberately aliases `DISPLAY_SCENE_LOCK_RELEASE_PENDING`, because update/callback paths propagate that state as a wait-and-retry result and `release_pending_display_lock` returns it when recursion prevents immediate release. The values are exact statuses, not a combinable result bitset. Raw `0x1000`, `0x2000`, and `0x3000` lock-mode comparisons in `display_scene.cpp` now use the existing release-pending, acquired, and mode-mask names. `set_display_clip_rectangle` and `release_pending_display_lock` use direct named returns; their callers still discard the result.
- All 366 enum members under `src` and `tools` use uppercase snake case. This applies consistently to scoped behavioral enums (for example `ApplicationAction::ENTER_FULLSCREEN`) and unscoped flag/protocol enums (for example `RUNTIME_MEDIA_PAUSED` and `SCRIPT_SCOPE_GLOBAL`); enum type names remain Pascal case.
- The remaining identifiable flag and placeholder-name decompilation residue has been cleaned up across the application, display, runtime, script/tree, resource, media, archive, and XTET code. Behavior-specific unscoped enums now name each independent bit domain; tests, sets, and clears use enum values with `&`, `|`, and `& ~` instead of hexadecimal masks. Trivial flag-mutator and empty animation-chunk wrappers were inlined and removed. Used offset-derived fields and operations were renamed for their observed roles (including resource variant, runtime suspension, pending display lock, inventory-pack membership, mouse state, volume, inventory name, and exception text); storage with no observable behavior is explicitly named `reserved_<offset>` rather than assigned a speculative meaning. Script scope tokens now use `ScriptScopeCode`. Source audits find no `unknown_*`/`field_*` identifiers and no direct hexadecimal operands in flag set/clear operations. The Debug build, `check-format`, and `git diff --check` pass.
- Repository formatting now sets `RemoveBracesLLVM: true`. Formatting removes optional braces from simple single-statement control bodies according to clang-format's conservative LLVM rules, while retaining braces for asymmetric or potentially ambiguous control flow. No additional manual brace-removal policy is applied.
- SDL key-down/up events pass through `translate_sdl_keycode`, which translates SDL keycodes into the legacy Win32 virtual-key numeric contract expected by the runtime and XTET (for example Escape `0x1b`, Space `0x20`, and arrows `0x25`-`0x28`). SDL mouse-button identifiers similarly pass through `translate_sdl_mouse_button` into the platform-neutral `RuntimeMouseButton` enum. These are platform-boundary compatibility mappings, distinct from text entry: printable text arrives through `SDL_EVENT_TEXT_INPUT`, while key-down enqueues only Backspace, Tab, Enter, and Escape into the main runtime byte queue and supplies control/gameplay keys to XTET.
- The project-owned C++ namespace is `freegag`; no `gag` namespace references remain under `src` or `tools`. Nested anonymous namespaces have been removed from both `freegag` and `xtet`, so their former helpers are direct named-namespace members. The resulting XTET `tokenize` collision was resolved with the purpose-specific names `tokenize_asset_manifest` and `tokenize_scene_description`. The only remaining anonymous namespaces are standalone global scopes in the resource extractor and XTET integration. The Debug build, `check-format`, namespace audits, and `git diff --check` pass.
- Proposed authentic nostalgia mode (not yet implemented): represent the 640x480 logical display as one authoritative 8-bit indexed root plus a 256-entry active palette, select script resource variant 0, and let the existing indexed-to-indexed compositor remap each resource's private palette into that shared palette. Keep SDL, its texture, and the platform window XRGB8888; expand the indexed presentation front through the active palette immediately before snapshot/upload. Palette-only updates must invalidate/re-expand the whole visible front, and the presenter's root/front distinction must remain indexed (or otherwise retain equivalent index/blackout state) so reveal, blackout, fades, and palette cycling remain correct. The current per-scene indexed backing alone is insufficient because an identical index can denote different RGB colors in different resource palettes. A single startup-only `ColorMode=HighColor|Indexed256` setting would express the real user choice more accurately than independently selecting low-color resources; a boolean `IndexedColorMode` is the simpler schema alternative. This remains a design direction, not current project behavior.
- The old 256-color/high-color choice distinguished destination/display behavior, not CDF bitmap encoding. An exhaustive header audit found all 462 `.BMP` records in `Gag01.cdf` and all 628 in `Gag02.cdf` are uncompressed 8-bpp indexed BMPs with 40-byte `BITMAPINFOHEADER`s; all 94 `.256` records per archive use the same encoding. Their palettes use four-byte BMP `RGBQUAD` records in B, G, R, reserved order: color precision is 24 bits although each entry occupies 32 bits on disk, and the runtime deliberately ignores the fourth byte even where archive data leaves it nonzero. Ordinary assets contain 256 entries (1024 palette bytes) and start pixels at byte 1078; bitmap-class `FONT2.RUS` is the exception with 121 entries and a byte-538 pixel offset. Each archive contains that font record twice. With a 16-bit RGB565 destination, `build_indexed_to_16_palette` expands every source index through that resource's private palette, allowing the combined frame to contain colors from many independent 256-entry palettes. With an 8-bit destination, `build_indexed_to_indexed_palette` instead finds the nearest entry in the single active destination palette; `/F:NOPAL` resources deliberately avoid installing a competing palette in 8-bit mode, while the same guard permits their palettes when the destination exceeds 8 bits. Primary 8-bit resources install 236 palette entries. Thus there is no recovered 16-to-8 bitmap conversion: the low-color variants are authored/quantized alternatives intended to survive shared-palette display constraints, whereas the high-color variants remain compact indexed sources whose private colors are expanded at composition time.
- A direct CDF/script audit verified the scope of the old color-mode resource selector. `Gag01.cdf` has 94 `.256` records/93 unique names and `Gag02.cdf` has 94 records/92 unique names; duplicates account for the record differences. The scripts actively select 92 and 91 unique `.256` names respectively as variant `:0`, covering 25 cursors, 64 inventory/menu assets, the full-screen help image, the video frame, and (on disc 1) a fly-clock image. `I-ITEMS.256` is present but not selected; the inventory bar instead pairs `Inv256.bmp:0` with `I-Items.bmp:1`. Disc 2 additionally selects three `.MOV:0` crocodile-animation variants against `*W.mov:1`, so the complete selector scope is 93 unique variant-0 resources on disc 1 and 95 on disc 2. All `.256` payloads are 8-bit BMP data despite their extension, and the direct `.BMP` alternatives are also 8-bit source files; 87 of 88 direct-name pairs render different art, with only `V-FRAME` byte-identical. This confirms that `LowColorResources` controls a broad low-color artwork/media set, not merely cursors and the inventory menu, while remaining independent from the modern XRGB8888 host framebuffer.
- The former hardcoded `ModernWindowsColorMode { indexed_8, rgb565_16 }` resource choice is restored as `[Game] LowColorResources`, default false. True preserves the old indexed-8 choice by leaving `ScriptRuntimeRoot::state_value_0824` at variant 0; false selects the old high-color variant 1. Indexed bitmap/animation/palette sources remain supported, while final composition and SDL presentation deliberately stay XRGB8888 rather than recreating an 8-bit host framebuffer. Debug build, formatting, diff checks, and canonical first-run INI smoke creation pass; manual low-color content coverage remains pending.
- `freegag.ini` now uses canonical `[Game] Fullscreen/IntegerScaling/LowColorResources/Sound/Subtitles` booleans instead of the legacy `Settings` bitmask. Values accept case-insensitive `true` and `false`; missing or invalid values use their defaults. Fullscreen, LowColorResources, and Subtitles default false, while IntegerScaling and Sound default true. `Sound=false` maps to the internal mute flag; the obsolete `Mute` key is unrecognized and discarded on rewrite. A missing file is created immediately with only the five game keys. Rewrites discard legacy, unknown, malformed, and comment entries; a complete valid `[Window] Bottom/Left/Right/Top` rectangle is the only optional state retained. Missing or invalid geometry centers the hidden native 640x480 SDL window on the primary display, and a normal windowed exit then persists its rectangle. The in-game option remains labeled Comments but reads and writes the positive `Subtitles` value: application bit `0x02000000` means enabled, while script `NOCOMMENT` is its inverse. Fresh-directory smoke launches verified the canonical default file; Debug build, formatting, and diff checks pass. Manual option toggles, low-color content, both scaling modes, window centering, and geometry persistence remain pending.
- A 2026-08-23 multiplatform-residue audit found no direct first-party Win32 runtime dependency: no Windows headers/APIs, native handles, calling conventions, Windows system-library linkage, or Windows-only game-target branches remain. The port is nevertheless verified only with MSVC x64 on Windows; there is no non-Windows CI/preset or recorded Linux/macOS configure, build, and run. Remaining portability work is chiefly verification plus host-path normalization (several legacy helpers recognize only `\\`), explicit little-endian CDF/SFS scalar serialization instead of native-memory reads/writes, and broader compiler/library validation. Cosmetic/content-compatibility remnants include inactive registry/CD error strings, the script-visible `XTETDLL.DLL` identifier, and the extractor's PE `win32_version` field; these are not runtime platform dependencies. The extractor's use of C++23 `std::println` requires a sufficiently recent standard library.
- Annotated tag `sdl-port-completable-2026-08-23` marks commit `6431c00dd550cfe719685db374de714502591114` as the known playable and end-to-end completable checkpoint after the full SDL platform port. The tag is local and has not been pushed.
- Verification preference: use a passing Debug build as the routine build check. Do not build Release on every change; reserve it for explicit requests or release-specific investigation.
- SDL startup now uses `SDL_EnterAppMainCallbacks` with the `waitevent` callback rate. SDL therefore services the same typed host-event path from its native live-move/live-resize iteration instead of using an expose-event watch that advanced the snapshot queue while semantic host events accumulated. Presentation notifications are coalesced, queued snapshots retain their order, and resize/expose repaint requests reuse the uploaded GPU texture without copying or uploading an unchanged framebuffer. Windowed and fullscreen presentation share the persisted `IntegerScaling` choice: true uses `SDL_LOGICAL_PRESENTATION_INTEGER_SCALE`, while false uses aspect-preserving `SDL_LOGICAL_PRESENTATION_LETTERBOX`. Debug builds pass for the game, XTET library, and resource extractor; `check-format` and both staged and unstaged `git diff --check` pass. Manual sustained video drag, next-video transition, edge resize, both scaling modes, and fullscreen confirmation remain pending.
- Runtime pause is cooperative engine orchestration, not an operating-system suspension. `0x01000000` requests the script executor's command loop to stop advancing script commands while it explicitly pauses XTET through operations 2/4, holds all animation workers through `runtime_animation_control_flags`, and suppresses/resumes SDL audio output. Stock scripts require no pause-specific logic; embedded gameplay modules must implement the host pause/resume operations. Matching the original window policy, the SDL host pauses only on minimization and resumes on restoration; a visible game continues regardless of keyboard focus.
- Subtitle suppression is restored at both existing generic-text attachment paths. Session reset clears `ScriptRuntimeRoot::flags`, so startup reapplies the persisted `NOCOMMENT` preference before constructing the selected script tree, when animation `/TEXT` attachments are decided, and afterward so script-file defaults cannot override it. Resource rebuilding skips animation-attached text while `NOCOMMENT` is set, and the script `TEXT` opcode skips interaction-triggered text attachment while continuing the surrounding event and audio commands. Hover-comment mechanics are unchanged. Debug and Release builds pass for the game, XTET library, and resource extractor, and `check-format` plus `git diff --check` pass. The user confirmed video subtitles were suppressed by the first half; manual confirmation of object-interaction subtitle suppression remains pending.
- The graphics lifecycle now restores the pre-XRGB invariant: each ordinary logical scene has one authoritative XRGB composition node with a persistent scene-sized indexed backing plane shared by its bitmaps and animations. Bitmap writes seed both representations; animation delta decoders update the shared indexed history and materialize only their dirty region into XRGB; palette changes rematerialize from that history. Scene clears zero both representations, while `NOCLS` simply omits the clear, so identical, shifted, resized, permanent, control, bitmap, and animation handoffs follow scene lifetime rather than resource geometry. Truly separated/private/load-only resources that receive distinct scene semantics remain independent indexed sources.
- The temporary retained-ownerless-surface implementation, exact-geometry adoption rule, special destruction API, and top-level retained-overlay purge have been removed. XRGB scene resizing allocates all replacement buffers before swapping them in and cannot leave mismatched indexed dimensions or destroy a live scene on allocation failure. Debug and Release builds pass for the game, XTET library, and resource extractor, and `check-format` plus `git diff --check` pass. Manual regression of the basement snake sequence, full magic-book spell chain (especially the moved final animation), and other `NOCLS` transitions remains pending.
- SDL now creates the application window hidden. Startup applies the validated saved window rectangle or requested initial fullscreen state and explicitly shows the window only afterward, preventing the default 640x480 frame from flashing before the persisted geometry takes effect. A show failure follows the existing graphics initialization cleanup path. Debug and Release builds of the game, XTET library, and resource extractor pass, along with `check-format` and `git diff --check`; manual startup confirmation remains pending.
- The speculative XTET result-dismissal latch was fully reverted; the original two-second result input behavior remains unchanged. A manual trace proved that a post-deadline click acquired the gameplay mutex, entered result state, and requested termination, but no host callback ran. The loader registered XTET's host and input-drain callbacks immediately before `initialize_game`, whose initial `shutdown_game` call then cleared both registrations. Callback registration is now persistent host configuration and is no longer cleared with game-instance state. All temporary instrumentation and its generated log were removed. Debug and Release builds of the game, XTET library, and resource extractor pass, along with `check-format` and `git diff --check`; manual game-over exit confirmation remains pending.
- Save-name submission by Enter regressed because printable input arrived through SDL text events, but translated key-down events were ignored by the main runtime byte queue; SDL does not emit text input for Enter or Backspace. Runtime key-down now restores the former translated-character behavior for Backspace, Tab, Enter, Escape, and keypad Enter while printable characters remain text-event-only to avoid duplication. Debug and Release builds, `check-format`, and `git diff --check` pass; manual Enter submission and editing confirmation remains pending.
- Startup incorrectly locked the main-menu Load action despite working-directory `.GSF` saves because the portable path convention uses an empty installation prefix, while `std::filesystem::directory_iterator("")` does not enumerate the current directory on Windows. Startup save detection and scripted save/load enumeration now normalize only an empty directory operand to `"."`; saved and loaded filenames remain bare working-directory-relative paths. Debug and Release builds, `check-format`, and `git diff --check` pass; manual confirmation that existing saves unlock and populate Load remains pending.
- Fullscreen/windowed transitions now preserve an in-game pointer location across geometry changes. Before requesting fullscreen, the presenter captures the pointer in 640x480 logical coordinates; after SDL emits the matching enter/leave-fullscreen event, it converts that point through the new letterboxed viewport and warps the OS pointer there. Duplicate requests for the same pending transition do not overwrite the capture. Window leave explicitly shows the system cursor and sends pointer-leave, while window enter reconstructs the current logical pointer event and restores either the software game cursor or the system cursor according to viewport hit-testing. Debug and Release builds, `check-format`, and `git diff --check` pass; manual fullscreen/windowed and leave/re-enter confirmation remains pending.
- The first intro-video completion exposed a self-join in the portable animation lifecycle: the terminal callback destroyed its resource on the animation worker, and `destroy_runtime_media_backend` attempted to join that same backend-owned `std::jthread`. Terminal callbacks now enqueue destruction for the script/runtime owner, return through the animation worker's exit path, and are then joined and destroyed by that owner. The queue is drained in the script loop, command loop, and resource-count waits; wait counts are decremented only after joined destruction completes. No animation thread is detached. Debug and Release builds, `check-format`, and `git diff --check` pass; manual confirmation past the first intro remains pending.
- The Windows-detachment implementation is complete in first-party code while Windows remains the only configured/verified host. SDL owns the sole window, event loop, input translation, cursor, geometry, fullscreen, presentation, and audio. Fullscreen remains available only through the scripted game menu; there is no Alt+Enter shortcut.
- First-party `src` code contains no `windows.h`, Win32 API types, calling conventions, messages, APIs, compatibility macros, Windows-only compile definitions, or direct `user32`/`gdi32` linkage. SDL's internal platform dependencies are intentionally untouched.
- Runtime and XTET input cross a typed `RuntimeInputEvent` boundary. Script-originated host commands use `ApplicationAction`, and the host dispatcher wakes through a registered SDL user event.
- Owned workers use joined `std::jthread`; runtime locks use recursive standard mutexes; display gates use a portable manual-reset event; timing uses standard chrono/sleep; and tracked zeroing/reallocation/release-all behavior is provided by `RuntimeHeap`.
- CDF physical I/O uses narrow exception-free binary stream interfaces backed by one `std::ifstream` or `std::fstream`. Asynchronous records share owned stream state, retain independent logical offsets, and preserve the existing ring-buffer chunking and pacing without reopening a file for each read.
- Project BMP, palette, and rectangle layouts use fixed-width portable types. Save-preview BMP headers are explicitly encoded and decoded as little-endian bytes; the indexed thumbnail payload remains binary-compatible.
- Debug and Release MSVC builds pass for the game, XTET library, and resource extractor. `check-format`, the first-party Win32 residue audit, and `git diff --check` pass. Manual regression coverage for this detachment pass remains outstanding.
- Startup is fully detached from the Windows registry. There are no production `Reg*` calls, registry API seams, installation/version checks, or logical-drive/CD discovery.
- The legacy archive/CD read-speed benchmark and startup speed gate have also been removed, including their API and tests.
- `ApplicationState::installation_path` is initialized to an empty prefix. `Gag01.cdf`, `XTETDLL.SFS`, autosaves, and generated `.GSF` paths resolve relative to the process working directory.
- Runtime settings and modern-window geometry persist in `freegag.ini` in the working directory. The file uses canonical `[Game] Fullscreen/IntegerScaling/LowColorResources/Sound/Subtitles` booleans and an optional `[Window] Bottom/Left/Right/Top` rectangle; invalid or unavailable values fall back nonfatally.
- The optional `gag_resources.rc` build pipeline and `gag_resource_test` were removed. `src/resource_test.cpp` was deleted, and `advapi32` is no longer linked.
- Decompilation is considered complete; the project is now entering source cleanup and reorganization.
- First-party test sources, CTest targets, `GAG_TESTING` code, and explicit `*_for_testing` hooks remain intentionally absent. Tests may be reenabled later, but no restoration approach has been selected.
- Do not create, restore, or add tests unless the user explicitly instructs you to do so.
- Scripted in-game save/load is unconditional. `FREEGAG_IN_GAME_SAVE_LOAD`, the recovered native save/load dialog path, and the standalone Win32 `Save Screen` export have been removed.
- The former modern-desktop compatibility path is unconditional. `FREEGAG_WINDOWS_FIXES`, its legacy branches, and physical display-mode switching are removed; SDL implements desktop fullscreen.
- Source-level decompilation metadata has been removed: executable-address annotations, structure size/offset assertions, provenance wording, empty application hooks, and an address-derived save function name. Script container parsing now initializes its carried truth value instead of emulating uninitialized stack data.
- Debug-only source paths are intentionally absent. All `_DEBUG` blocks, trace helpers/calls, trace counters, release-mode no-op macros, and debug-only ignored-result temporaries have been removed.
- C++23 is required globally from the root CMake configuration, with compiler-specific language extensions disabled; individual targets do not repeat the standard requirement.
- The root CMake formatting targets use root-relative recursive globs to discover all project-owned `.cpp` and `.h` files under `src/` and `tools/`; per-directory formatter lists are removed, and in-tree build/dependency sources are excluded by scope.
- XTET is integrated as a static library. Its application-facing integration is implemented in `src/xtet/integration.cpp`; unused DLL entry-point typedefs and an empty architecture guard have been removed. The script and startup contracts were not changed as part of this cleanup.
- XTET has no native-platform API boundary. `src/xtet/api.h` consumes portable runtime input, presentation, audio, and lifecycle callbacks; timing and animation delays use standard chrono and threads.
- XTET initialization failures throw `std::runtime_error`. The existing startup exception boundary shuts XTET down and restores host state before rethrowing; XTET has no debugger-output or error-dialog path.
- The game uses the standard `main(int, char **)` entry point and is linked as a console application. `run_startup` detects `--xtet` directly from `argv` and initializes the SDL-owned application host.
- Project addons for scripted save/load and synthesized resources live under `src/extra/`: `save_load.*`, `synthesized_resource.*`, and `save_load_bg_patch.h`. They are listed directly by `src/CMakeLists.txt`; `src/extra` intentionally has no nested CMake file.
- The former startup monolith is split into stock-game modules directly under `src/`: `application`, `display_host`, `display_scene`, `runtime`, `script`, `runtime_tree`, `runtime_model`, `resource`, `media`, `text`, `audio`, and `game_host`. `startup.h` exposes only `run_startup`, and `startup.cpp` contains startup orchestration and the SDL event loop.
- Functional headers own subsystem declarations. Shared runtime layouts are preserved in ordered `*_types.h` headers, while mechanically shared legacy state and callback tables remain private to implementation files through `runtime_internal.h`; public consumers and addons do not include it.
- Architecturally, the game is a script-driven adventure runtime hosted by SDL. A dedicated script executor processes parsed runtime trees and coordinates the logical model, resources, scene compositor, media, text, audio, and statically embedded XTET minigame; the main thread owns SDL UI and events.
- SDL 3.4.10 is fetched from its release archive with a pinned SHA-256 hash and linked statically only to the game. SDL shared libraries, test components, examples, documentation, installation, and the file-dialog subsystem are disabled. The root project remains C++-only; SDL enables C within its own fetched project.
- Game audio uses one bound SDL3 audio stream per logical sound handle. Source WAV, animation, and XTET bytes remain in their original PCM format until SDL converts, resamples, and mixes them for a preferred float32 stereo 44.1-kHz playback device. The recovered integer converter, resampler, mixer, shared output blocks, and mixer-format switching are removed.
- Sound slots, queued segments, stream state, and fades are private to `audio.cpp`. Other subsystems use synchronized status snapshots and marker updates. Per-stream gain implements volume and a 5-ms `std::jthread` control loop drives float fades; the same worker advances source frames silently when SDL initialization, device opening, stream creation, or stream binding is unavailable.
- SDL audio-device opening occurs during sound initialization and reopening occurs during `resume_runtime_sound_output`. The temporary control-thread deferral and one-second retry machinery were an unsuccessful black-screen fix and have been removed after the actual content-validation fault was identified.
- The reported black screen was traced to a newly added generic animation-frame boundary validator rejecting the valid first frame of `LOGO1.mvz`. All content-validation rules introduced by the SDL audio refactor have been removed, and the temporary diagnostic logger, message scopes, counters, calls, and CMake entries have also been removed.
- Project code no longer contains WinMM audio/timer APIs or types and no longer explicitly links `winmm`; SDL may select WinMM-related Windows libraries transitively. WAV, animation, and XTET audio cross the game boundary through `RuntimePcmFormat`, with an explicit XTET adapter.
- All former `timeGetTime` and `GetTickCount` uses share `runtime_milliseconds()`, backed by `std::chrono::steady_clock` and returning the low 32 bits to preserve existing wraparound arithmetic.
- Runtime work is distributed across the UI thread, script executor, display-scene worker, SDL audio demand callback (or silent fallback worker), asynchronous file workers, and per-animation workers. Non-audio subsystems coordinate through shared state, standard mutexes/manual-reset events, and queues; game-domain UI-thread dispatch uses the portable host-event queue.
- Audio content retains the original engine's acceptance boundary. The refactored audio handle constructor keeps the legacy PCM/tag, mono/stereo, 8/16-bit, and minimum-rate checks, then derives block alignment and average byte rate as the old mixer did. Standalone WAV and animation audio traversal retain their pre-refactor assumptions; no new RIFF, frame-size, chunk-size, frame-alignment, or descriptor-consistency rejection is applied.
- A repository-data audit found CDF WAV inputs in PCM mono 8-bit 22.05 kHz, mono 16-bit 11.025 kHz, and stereo 16-bit 11.025 kHz formats; these descriptors and bytes now enter their SDL voice streams unchanged.
- Verified Debug and Release MSVC builds of the game, XTET static library, and resource extractor after restoring the legacy audio-content acceptance behavior. The repository `check-format` target and `git diff --check` pass. User verification of the reported black-screen/startup hang fix and broader manual audio coverage remain outstanding.
- The user has manually confirmed that audio appears to work correctly after the SDL stream conversion and cleanup. Detailed A/B assessment of perceptual differences from the legacy integer mixer has not been performed.
- SDL_mixer integration is intentionally deferred. Base SDL3 already owns conversion, resampling, float mixing, and device output; the remaining project audio code preserves game-specific handles, queues, looping, markers, silent progression, and animation synchronization.
- Final framebuffer presentation uses SDL3's sole application window, an automatically selected non-VSync renderer, and a nearest-filtered streaming XRGB8888 texture. SDL also owns input, cursor behavior, geometry, fullscreen, close, and quit events.
- The graphics pipeline now uses XRGB8888 for every writable composition and presentation destination: offscreen writable scenes, the compositor root, the visible front buffer, queued snapshots, standalone text and previews, and the XTET framebuffer. SDL receives the 32-bit rows directly without RGB565 expansion.
- Static bitmap/font resources and non-primary animation scenes remain indexed source surfaces. Indexed pixels are expanded through their own palette only while drawing into XRGB8888; the legacy destination-palette nearest-color remap and RGB565 packing paths are no longer active. Primary animations retain a private indexed history plane only when delta decoding into a writable XRGB destination requires it.
- Internal XRGB pixels use the ignored high byte as a keyed-visibility marker rather than alpha. This preserves index-zero transparency through XRGB-to-XRGB composition without reducing RGB precision.
- Savegame-embedded previews deliberately retain the original 8-bit indexed BMP structure and palette quantization so old and new saves remain format-compatible. There is no standalone user-screenshot path.
- Cross-thread presentation uses three reusable full-frame snapshots and the portable host-event dispatcher. SDL renderer calls stay on the UI thread, transition snapshots retain order, and presenter shutdown cancels blocked producers before the display worker is joined.
- DirectDraw, native display-mode enumeration/validation, GDI DIB framebuffer presentation, and unreachable GDI media hooks and context fields are removed. First-party code does not link GDI or user32.
- Fresh x64 MSVC Debug and Release configurations build the game, XTET static library, and resource extractor with the lazy XRGB graphics pipeline. `check-format` and `git diff --check` pass; manual intro, main-menu static hover substitutions, animation palette/delta outside the main menu, transition, text, save-preview round-trip, scaling, fullscreen, repaint, XTET, and forced-failure coverage remains pending.
- Resource construction assigns an explicit scene role from script semantics rather than from load order. Primary resources and ordinary shared static bitmaps bind writable XRGB8888 composition scenes; every non-primary animation and every separated, control, load-only, permanent, or private helper resource retains an indexed source scene. The logical scene index controls ordering and script lookup but is not unique pixel-storage identity: one XRGB composition scene and independent indexed sources may coexist at the same index, and index lookup prefers the composition scene while each source retains its exact scene handle.
- Main-menu hover highlights are static BMP resources activated with script `PLAY` commands. They bind the same XRGB composition scene as the menu background and expand directly into it. In the credits tree, the permanent 2x2 `Credits.mov` indexed source can now coexist at scene index 0 with the 640x480 `crd1.bmp` XRGB background instead of blocking its construction. This addresses the missing hover and missing credits-background failures without a new static data store or persistent 32-bit asset cache.
- Debug and Release builds of the game, XTET static library, and resource extractor pass after the explicit scene-role/binding fix. `check-format` and `git diff --check` pass. Manual verification of menu hover substitutions, the credits background and scrolling text, and surrounding transitions remains pending.
- The first XTET runtime check exposed a cross-thread lock cycle: its worker could hold the gameplay mutex while waiting for space in the bounded presentation queue, while the UI thread blocked on that mutex before it could service presenter messages. XTET window dispatch now ignores unrelated messages and uses a nonblocking gameplay lock for keyboard and mouse input; contended input is reposted behind already queued presenter work. Debug and Release game builds plus both XTET/resource-extractor configurations pass; user confirmation of the minigame fix remains pending.
- SDL hides its system cursor inside the logical game viewport while script cursors are software rendered. `GR.CIN` and `GRTV.CIN` intentionally bind the bitmap-less `MM` cursor during playback; outside the logical viewport SDL restores its system cursor.
- Script text colors are palette indices produced by remapping the indexed font-atlas masks. XRGB text drawing now resolves those indices through the active game/display palette, matching the original indexed destination semantics; resolving them through the font bitmap's private palette caused dark subtitles.
- XRGB-bound animations retain an animation-sized private indexed history canvas with local coordinates and stride. Delta decoding and palette changes operate on that canvas, and only its clipped dirty region is expanded at the animation's destination offset. The former full-scene zero-filled history could overwrite unrelated portions of a shared composition scene with black when an animation palette chunk was processed.
- Release builds of the game, XTET static library, and resource extractor pass after the subtitle and animation-history fixes. Debug compilation passes, and the Debug XTET and resource extractor targets link; the Debug game link is currently blocked only because the user has `build/src/Debug/freegag.exe` running. `check-format` and `git diff --check` pass. Manual confirmation of subtitle colors and the first-room picture animation remains pending.
- The user has completed an end-to-end playthrough after the SDL audio and framebuffer-presentation transitions. The current main-branch checkpoint is known completable and is marked by the annotated `sdl-completable-2026-08-23` tag.
- Custom native engine notifications `0x7ffb` through `0x7ffe` are removed. Game-domain work crosses threads through a typed standard C++ host-event queue with owned payloads, synchronous completion, early acknowledgement, shutdown cancellation, coalesced wakeups, and one registered SDL user-event doorbell.
- Runtime/application commands, queued presentation, animation failures, and XTET result/pause/resume/termination notifications use the host-event dispatcher. SDL input, focus, resize, move, close, and quit events are translated directly into portable runtime/application behavior.
- Debug and Release builds of the game, XTET static library, and XTET resource extractor pass after the host-event migration. `check-format` and `git diff --check` pass. Manual startup, transitions, save/load, fullscreen, animation, shutdown-under-load, and XTET verification remain pending.
- The missing thumbnail in the scripted save dialog was caused by script message `0xbc2` storing its pre-transition snapshot through the former 32-bit standalone screenshot path. It now uses `capture_save_game_bitmap`, preserving the legacy 8-bit indexed 320x240 BMP consumed by preview decoding and embedded as `COMMENT.BMP`. Debug and Release builds of the game, XTET library, and resource extractor pass; `check-format` and `git diff --check` pass. Manual save-dialog thumbnail display and save/load round-trip verification remain pending.
- The startup main menu incorrectly exposed Save after the host-event migration because owned `std::string` query names were passed directly into the legacy fixed-width script lookup. That lookup compares all 32 bytes, so bytes beyond `c_str()`'s terminator could prevent `MM.SAVE_DIS` from matching and create a duplicate field. Host query names are now rematerialized as zero-padded 32-byte fields before every lookup, preserving the original message behavior without additional save eligibility rules. Temporary startup tracing verified the original sequence: `3010` retained `NOSAVE`, query 1003 produced ON, and the existing `MM.SAVE_DIS` field became active; all tracing and its log were removed. Manual user confirmation remains pending.
- A follow-up compatibility audit compared every producer and handler removed or translated by the host-event migration, all owned payload/result conversions, XTET result/lifecycle delivery, presentation wakeups, synchronous completion and early acknowledgement, shutdown cancellation, and the affected API-table layouts/initializers. No additional fixed-width-name or payload-lifetime defect was found. Removed notification values were confirmed to have reached only default/no-op handlers. The remaining intentional ordering difference is that the C++ dispatcher is FIFO and drains all queued host events per coalesced doorbell, whereas Win32 gave cross-thread sent messages priority and interleaved separately posted messages with the platform queue; stress/manual coverage remains appropriate for that architectural change.
- Screenshot capture is now exclusive to scripted manual-save thumbnails: a half-resolution 8-bit indexed BMP is shown in the save UI and embedded as `COMMENT.BMP`; script message `0xbc2` may capture it before a transition. Loading reads that entry only for preview display, and `AUTOSAVE.GSF` contains no bitmap. The Options-menu `Save Screen` item, full-resolution 32-bit BMP serializer, unused legacy capture helpers/API slots, `GetSaveFileNameA`, `<commdlg.h>`, and the direct `comdlg32` dependency have been removed.
- The missing black areas in save/load previews were localized to preview materialization, not capture or `COMMENT.BMP`. `copy_preview_pixels` gave palette index 0 a zero XRGB visibility marker, so transparent layer composition skipped those thumbnail pixels and exposed the dialog background. Every decoded 32-bit preview palette entry is now marked visible, including index 0, while clearing the layer still writes marker-zero pixels so an absent preview remains transparent. Debug and Release builds plus `check-format` and `git diff --check` pass, and the user manually confirmed the affected save previews now render correctly.
- `RuntimeGameHostContext` has been reduced to its five live members: `bits_per_pixel`, `width`, `height`, `framebuffer`, and `palette_entries`. The untouched `reserved_0010`, unread framebuffer mirrors `reserved_0028`/`reserved_0030`, write-only `display_surface`, zero-only `x_offset`/`y_offset`, and all associated assignments are gone. The structure has no serialized or dynamic-library ABI role; Debug and Release builds, `check-format`, stale-member audit, `git diff --check`, and LF audit pass.
- `RuntimeMessagePair` and all `pair_*` input-queue terminology have been replaced with typed `RuntimeQueuedInput` records carrying `RuntimeQueuedInputType type` and `packed_position`; queue storage, locking, enqueue/dequeue/reset, and processing names now describe queued runtime input. `RuntimeInputSessionRecord` is gone: `RuntimeInputText` is a 32-byte `std::array<char, ...>`, and `take_runtime_input_text` returns it directly while retaining the cursor reset. The ignored cursor-status return and the save path's record-to-character-buffer copy are removed. Debug and Release builds, formatter/stale-symbol audits, `git diff --check`, and LF audit pass.
- Project-owned native dialogs have been removed. Archive validation, window-class registration, and fatal runtime-exit errors now report the existing localized messages to `stderr` instead of calling `MessageBoxA`, and SDL's optional file-dialog subsystem is disabled. Debug and Release builds of the game, XTET library, and resource extractor pass after this cleanup; `check-format`, `git diff --check`, and a source scan for common-dialog/message-box remnants pass. SDL's statically linked Windows video backend still contains its own unused simple-message-box implementation and therefore imports `MessageBoxA`; SDL exposes no build option to remove that backend function independently.
- The first attempted fly-minigame pacing fix, deferring playback completion until estimated audible time, did not change the reported behavior and was fully removed. A closer legacy comparison found that animation audio's schedule marker originally appeared only after one fixed mixer block (about 186 ms for supported game PCM), whereas the per-voice SDL callback marked it on the first submitted bytes; the animation snapshot path could also retain a stale pre-reset marker and skip its wait entirely. Per-segment schedule progress now preserves the legacy block-boundary handshake and the local snapshot is cleared with the live markers. The user confirmed that this restores fly movement speed. Temporary instrumentation then established that clicking starts `FLMx-ul2.mov` directly with no script `WAIT`; its second frame waits 189 ms for audio schedule readiness before presentation. The longer legacy-feeling click-to-stab pause came from the shared mixer's phase: a newly queued sound could wait up to one partial global block before consuming its first full block, producing roughly 186–372 ms of transport latency, while a new per-voice SDL stream requests immediately and consistently lands near the minimum. All temporary logging source and its generated log were removed. No compensating gameplay delay or shared-phase emulation has been added.
- A debugger capture of the black-dashboard car cutscene identified the same secondary-animation startup race as the fly-minigame hang. `ISLAND.CIN` builds both directions from a primary road/background bitmap, a 640x480 CONTROL FLC, and a 320x240 MOV scaled 2x. In the failed `CAR01` capture, the bitmap and `CR0102.MOV` shared the active XRGB scene and the MOV had reached frame 47, while `CR0101.FLC` remained at frame 0 with `PAUSED | PAUSE_NOTIFIED`. The first fly fix cleared pending registration by assigning a callback-time snapshot of the complete media flag word; if animation finalization resumed the worker concurrently, that stale assignment could restore its initial `PAUSED` bit. Pending registration now serializes with finalization through the existing media mutex, rereads live flags, and clears only `RUNTIME_MEDIA_RESOURCE_PENDING`, preserving the original resume transition. The Linux game build, `check-format`, and `git diff --check` pass. Manual verification of the fly minigame and both car directions remains pending; the game was not launched or resumed by the agent.
- XTET Down-key presses accumulated during a keyboard-triggered match animation can still affect the next figurine. Traces established that SDL translates the Linux key events only after the blocking match callback and after attempted callback/iterate drain boundaries, unlike the original Win32 path that removes pending keyboard messages before returning from the synchronous match handler. The timestamp filter, deferred suppression states, callback-rate manipulation, one-event-at-a-time event-loop experiment, and all `[xtet-drain-*]` diagnostics were unsuccessful and have been fully reverted. The prior SDL `waitevent` callback host and recovered public-queue keyboard flush are restored; no Tetris behavior change remains. A future fix needs a fresh design rather than extending these drain-boundary experiments. The Linux game and `check-format` targets build and `git diff --check` passes. The game was not launched by the agent.
